<h2>Welcome, <%=current_user.username %></h2>
<h3>Your Shopping List</h3>
  <% @items.categories.each do |item| %>
<div>
<p> <span class="pull-left"> <%=@item.quantity%> </span><span class="pull-right"> <%=@item.name%> </span></p>
</div>
  <%end%>

  <br><br>

  <a href ='items/new' class='btn btn-default'>Add Items</a>
    <a href ='/logout' class='btn btn-default'>Log Out</a>
